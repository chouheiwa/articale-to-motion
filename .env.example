# ArticleToMotion 工具配置
# 取值优先级：环境变量 > 本文件 > article-to-motion.conf > 内置默认
# 编排工具：codex | claude | qoder | codebuddy | opencode
ORCHESTRATOR=codex
# 渲染工具：codex | claude | qoder | codebuddy | opencode（可与 ORCHESTRATOR 相同）
RENDERER=claude
# TTS 工具：minimax | bailian（完整制作流程需要）
TTS_PROVIDER=minimax
# 镜头并发数：1-16，默认 3
SCENE_JOBS=3

# 本文件中的所有键都会注入渲染与 TTS 子进程的环境。
#
# 密钥不得写入本文件。MiniMax 用 `mmx auth login --api-key sk-xxxxx`，
# 百炼用 `bl auth login --console`，凭据分别保存在 ~/.mmx/config.json 和
# ~/.bailian/config.json，不进入项目目录。

# MiniMax 非密钥配置（TTS_PROVIDER=minimax 时可选）
# MINIMAX_TTS_MODEL=
# MINIMAX_VOICE_ID=

# 阿里云百炼非密钥配置（TTS_PROVIDER=bailian 时可选）
# BAILIAN_TTS_MODEL=
# BAILIAN_VOICE_ID=
